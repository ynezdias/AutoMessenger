__pycache__/
*.pyc
server/.local_conversations.json
aws/packaged.yaml
.aws-sam/
logs/

# TEMPORARILY UN-IGNORED to move config to another machine.
# server/.env and aws/deploy-params.txt hold live AWS keys and the webhook
# secret. Once they are committed to a remote they stay in history forever, so
# re-ignore them and rotate both credentials as soon as the transfer is done.
# server/.env
# aws/deploy-params.txt
